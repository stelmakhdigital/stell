upstream hands {
    server runtime:8081;
}

server {
    listen 8081;
    location /healthz {
        proxy_pass http://hands;
    }
    location / {
        proxy_pass http://hands;
        proxy_set_header Host $host;
        proxy_read_timeout 130s;
    }
}
